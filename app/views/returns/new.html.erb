<div class="page-header">
  <%= link_to insurance_returns_path(@return.insurance), class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <h1>New return</h1>
</div>

<%= render 'form' %>
