<div class="row">
  <div class="col-lg-12">
      <%= form_for([@book.insured_user, @book], html: {class: "form-horizontal"}) do |f| %>
      <% if @book.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

          <ul>
          <% @book.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :number, class: 'col-lg-2 control-label'  %>
        <div class="col-lg-10">
          <%= f.text_field :number, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :begin_at, class: 'col-lg-2 control-label'  %>
        <div class="col-lg-10">
          <%= f.datetime_select :begin_at, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :end_at, class: 'col-lg-2 control-label'  %>
        <div class="col-lg-10">
          <%= f.datetime_select :end_at, :include_blank => true, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <div class="col-lg-offset-2 col-lg-10">
          <%= f.submit 'create book', class: "btn btn-primary" %>
        </div>`
      </div>
    <% end %>

  </div>
</div>
