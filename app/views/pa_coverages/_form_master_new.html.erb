<div class="row">
  <div class="col-lg-12">
    <%= form_for(@pa_coverage, url: master_pa_coverages_path(@pa_coverage), html: {class: "form-horizontal"}) do |f| %>
      <% if @pa_coverage.errors.any? %>
        <div id="error_exinsuranceation">
          <h2><%= pluralize(@pa_coverage.errors.count, "error") %> prohibited this pa_coverage from being saved:</h2>

          <ul>
          <% @pa_coverage.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :key, class: 'col-lg-2 control-label' %>
        <div class="col-lg-10">
          <%= f.text_field :key, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :value , class: 'col-lg-2 control-label'%>
        <div class="col-lg-10">
          <%= f.text_field :value, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :description, class: 'col-lg-2 control-label' %>
        <div class="col-lg-10">
          <%= f.text_area :description, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :tag_list, class: 'col-lg-2 control-label' %>
        <div class="col-lg-10">
          <%= f.text_field :tag_list, :value => "#{@pa_coverage.tag_list}", class: "form-control thunder-taginput", "data-role" => "tagsinput" %>
        </div>
      </div>
      
      <div class="form-group">
        <div class="col-lg-offset-2 col-lg-10">
          <%= f.submit 'submit', class: 'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
</div>

