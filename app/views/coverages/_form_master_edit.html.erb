<div class="row">
  <div class="col-lg-12">
    <%= form_for(@coverage, url: master_coverage_path(@coverage), html: {class: "form-horizontal"}) do |f| %>
      <% if @coverage.errors.any? %>
        <div id="error_exinsuranceation">
          <h2><%= pluralize(@coverage.errors.count, "error") %> prohibited this coverage from being saved:</h2>

          <ul>
          <% @coverage.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.label :key, class: 'col-lg-2 control-label' %>
        <div class="col-lg-10">
          <%= f.text_field :key, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :value , class: 'col-lg-2 control-label'%>
        <div class="col-lg-10">
          <%= f.text_field :value, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :description, class: 'col-lg-2 control-label' %>
        <div class="col-lg-10">
          <%= f.text_area :description, class: "form-control" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :tag_list, class: 'col-lg-2 control-label' %>
        <div class="col-lg-10">
          <%= f.text_field :tag_list, :value => "#{@coverage.tag_list}", class: "form-control thunder-taginput", "data-role" => "tagsinput" %>
        </div>
      </div>
      
      <div class="form-group">
        <div class="col-lg-offset-2 col-lg-10">
          <%= f.submit 'submit', class: 'btn btn-primary' %>
        </div>
      </div>
    <% end %>
  </div>
</div>

