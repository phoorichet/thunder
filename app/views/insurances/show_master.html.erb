<div class="row">
  <div class="col-lg-12">
    <h3>Master Plan</h3>

    <%= link_to 'Edit', edit_master_insurance_path(@insurance) %>

    <p>
      <strong>Name:</strong>
      <%= @insurance.name %>
    </p>

    <p>
      <strong>Minimum Age:</strong>
      <%= @insurance.minimum_age %>
    </p>

    <p>
      <strong>Maxmimum Age:</strong>
      <%= @insurance.maximum_age %>
    </p>

    <p>
      <strong>Consider Year:</strong>
      <%= @insurance.consider_year %>
    </p>

    <p>
      <strong>Consider Gender:</strong>
      <%= @insurance.consider_gender %>
    </p>

    <p>
      <strong>Paid Period Lenght:</strong>
      <%= @insurance.paid_period_length %>
    </p>

    <p>
      <strong>Protection Length:</strong>
      <%= @insurance.protection_length %>
    </p>

    <p>
      <strong>Group:</strong>
      <%= @insurance.group %>
    </p>

    <p>
      <strong>Company:</strong>
      <%= @insurance.company %>
    </p>

    <p>
      <strong>Tags:</strong>
      <%= @insurance.tag_list %>
    </p>

    <h3>Assoiated Riders</h3>
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th>Status</th>
          <th>Code name</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @insurance.riders.each do |rider| %>
          <tr>
            <td><%= rider.name %></td>
            <td><%= rider.description %></td>
            <td><%= rider.status %></td>
            <td><%= rider.code_name %></td>
            <td><%= link_to 'Show', [rider.insurance, rider] %></td>
            <td><%= link_to 'Edit', edit_insurance_rider_path(rider.insurance, rider) %></td>
            <td><%= link_to 'Destroy', [rider.insurance, rider], method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <%= link_to 'Add new rider', new_insurance_rider_path(@insurance) %>

    <br>
    <br>
    <hr>

    <%= link_to 'Back to Master Plan', masters_insurances_path %> 
  </div>
</div>


