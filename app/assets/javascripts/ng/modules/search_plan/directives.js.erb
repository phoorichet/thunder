'use strict';

(function(){

  var module = angular.module('thunder.searchPlan.directives', ['thunder.searchPlan.controllers']);
  
  module.directive('searchPlanPanel', function(){
    return {
      restrict: "AE",
      replace: true,
      // template: "<p>Test Directive hahaha</p>",
      templateUrl: "<%= asset_path 'ng/modules/search_plan/search.html' %>", //'/assets/ng/modules/widget/template.html',
      scope: {
        uid: '=',
      },
      controller: 'SearchPlanController',
      link: function(scope, element, attributes){        
        // Set scope attrbute so that we can use in the template
        scope.attributes = attributes;
        scope.element = element;

        scope.id = attributes.id;

        // $('.item-modal').on('loaded.bs.modal', function (e) {
        //   console.log(e.target);
        // })

      }
    }
  });

}());