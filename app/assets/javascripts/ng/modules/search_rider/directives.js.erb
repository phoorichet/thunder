'use strict';

(function(){

  var module = angular.module('thunder.searchRider.directives', ['thunder.searchRider.controllers']);
  
  module.directive('searchRiderPanel', function(){
    return {
      restrict: "AE",
      replace: true,
      // template: "<p>Test Directive hahaha</p>",
      templateUrl: "<%= asset_path 'ng/modules/search_rider/search.html' %>", //'/assets/ng/modules/widget/template.html',
      scope: {
        uid: '=',
      },
      controller: 'SearchRiderController',
      link: function(scope, element, attributes){        
        // Set scope attrbute so that we can use in the template
        scope.attributes = attributes;
        scope.element = element;

        scope.id = attributes.id;

        // $('.item-modal').on('loaded.bs.modal', function (e) {
        //   console.log(e.target);
        // })

      }
    }
  });

}());