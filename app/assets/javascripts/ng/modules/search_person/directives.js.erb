'use strict';

(function(){

  var module = angular.module('thunder.searchPerson.directives', ['thunder.searchPerson.controllers']);
  
  module.directive('searchPersonPanel', function(){
    return {
      restrict: "AE",
      replace: true,
      // template: "<p>Test Directive hahaha</p>",
      templateUrl: "<%= asset_path 'ng/modules/search_person/search.html' %>", //'/assets/ng/modules/widget/template.html',
      scope: {
        uid: '=',
      },
      controller: 'SearchPersonController',
      link: function(scope, element, attributes){        
        // Set scope attrbute so that we can use in the template
        scope.attributes = attributes;
        scope.element = element;

        scope.id = attributes.id;
        console.log(scope.id);

        // $('.item-modal').on('loaded.bs.modal', function (e) {
        //   console.log(e.target);
        // })

      }
    }
  });

}());